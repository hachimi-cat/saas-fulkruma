{
  "name": "@forjio/fulkruma-cli",
  "version": "0.3.0",
  "description": "CLI for Fulkruma (part of the Forjio commerce suite).",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "fulkruma": "bin/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "tsc -p tsconfig.build.json --watch",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@forjio/fulkruma-node": "^0.2.0",
    "@forjio/sdk": "^0.5.0",
    "chalk": "^5.6.2",
    "commander": "^12.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "files": [
    "dist",
    "bin"
  ],
  "engines": {
    "node": ">=20.0.0"
  }
}
